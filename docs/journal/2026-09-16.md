# 2026-09-16 — Project Writing Stage 8 Findings Resolved, Backend Committed, Frontend Milestone Started and Closed Out Through Stage 10

## Session: Resolving Both Stage 8 Findings (RUNNING Work Item Recovery; Instructions Contract)

**Objective:** Stage 8 (2026-09-15) validated the Project Writing backend end to end but surfaced two findings without resolving them, per its validation-only scope: (1) a `RUNNING` Work Item that crashes mid-processing is never automatically retried - a general, pre-existing ADR-006 characteristic, not Writing-specific; (2) `docs/Project_Writing_Implementation_Plan.md` §19 item 3's 2026-09-12 decision (generation instructions persisted via a Draft-scoped Conversation/Message) was never actually implemented - Stage 7 shipped a plain `instructions` request field instead. Explicitly authorized to resolve both, with direction that the first be treated as a general worker/ADR-006 concern rather than a Writing-specific patch.

### RUNNING Work Item Recovery

Added `WorkItemRepository.requeue_stale_running(stale_after, max_attempts)` (`app/workers/repository.py`) - kind-agnostic (Writing and Knowledge Processing Work Items both benefit), reusing `mark_failed`'s own bounded-retry rule so a Work Item that keeps crashing its worker still terminates in `failed` rather than looping forever. `WorkItemExecutorLoop.start()` (`app/workers/executor.py`) now calls it once, synchronously, before polling begins - deliberately not on every poll tick, so it can never race the item the current process is itself still executing; a stale `RUNNING` row can only be left behind by a *prior* process that crashed or was killed. Default threshold: 10 minutes, configurable via a new `stale_running_threshold` constructor parameter.

### Instructions Contract Resolution

Re-read §19 item 3's original decision text before choosing a resolution path: the 2026-09-12 approval was for Option (b) specifically - persist instructions via the frozen `Conversation`/`Message`/`Message Context Link` entities (04_Logical_Data_Model.md §3.9, §3.10, §4.4), which Stage 2 explicitly deferred building. Rather than re-litigating that decision or retroactively blessing Stage 7's direct-field shortcut, implemented Option (b) as originally specified:

- `Conversation`, `Message`, `MessageContextLink` added as domain entities (`app/modules/writing/domain/entities.py`), ORM models (`infrastructure/models.py`), and repositories (`infrastructure/repositories.py`), following the exact conventions already established for `DraftEvidenceLink`/`MemoryProvenanceLink` (exclusive-arc CHECK constraints, `_enum_column` value-callable pattern).
- `MemoryProvenanceLink.conversation_id` gained the real foreign key it was missing since Stage 2 (deferred at the time because Conversation didn't exist yet) - a small, low-risk consistency fix now that the referenced table is real.
- `RequestDraftGenerationUseCase` (`app/modules/writing/application/generation_request.py`) now finds-or-creates a Conversation per Draft (via the genuinely free-form `title` field, keyed `draft:<id>:instructions` - deliberately not a new `draft_id` column, so the frozen Conversation shape stays untouched) and persists each generation request's instructions as that Conversation's next `Message`, before enqueueing generation.
- The public HTTP contract is unchanged: `RequestDraftGenerationRequest.instructions` is still a plain string field - only the internal persistence mechanism changed, not the client-facing API.
- `EnqueueDraftGenerationUseCase.execute()` and `build_generate_draft_version_payload_reference`/`parse_generate_draft_version_payload_reference` (`app/workers/payloads.py`) gained an optional `message_id`, threaded through the Work Item's `payload_reference` (a new 4th colon-delimited segment; a genuine, intentional breaking format change - no backward-compatibility shim was added for the dev-only SQLite database, consistent with this project's stated preference against such shims).
- The executor's writing branch (`app/workers/executor.py`) now links the instructions Message - and a newly-created system-response Message recording which Draft Version resulted - to that Draft Version via `MessageContextLink`, closing the provenance loop from "what was asked for" to "what was produced."

### Verification

New tests at every layer: domain validation (`Conversation`/`Message`/`MessageContextLink` exclusive-arc and blank-content rejection), unit tests for `RequestDraftGenerationUseCase` (instructions persisted as a Message, Conversation reused across multiple requests for the same Draft, message_id round-trips through the payload reference), and integration tests for `requeue_stale_running` (recovers a stale item, ignores a freshly-claimed one, terminates a poison item once attempts are exhausted, ignores non-`running` states) plus a full `WorkItemExecutorLoop.start()`-to-completion run proving the recovery hook is actually wired, not just unit-tested in isolation, and a full writing-generation run proving the executor genuinely creates both `MessageContextLink` rows and the system-response `Message`. Full regression: 524/524 tests pass (512 prior + 12 new), zero regressions.

### Documentation

Updated `docs/Project_Writing_Implementation_Plan.md` (§18 Stage 8 entry and §19 item 3 both got an appended `RESOLVED 2026-09-16` note - the original findings were preserved, not rewritten, per this project's governance discipline of correcting/appending rather than erasing prior recorded findings; top Status line updated), `docs/Traceability_Index.md` (Project Writing bullet updated to record both resolutions), and this journal entry.

## Session: Frontend Milestone Started, and a Real Manual Workflow Validation

**Objective:** Committed the validated Project Writing backend, then started the Frontend milestone against it (project owner directed the stack - React 19 + TypeScript + Vite, TanStack Query, React Router, Tailwind + shadcn/ui, Zod, Vitest/Testing Library, Playwright - and a 10-stage plan), then ran the full real manual workflow (login → project → documents → knowledge processing → writing style → generation → review → regeneration) through the actual built frontend against a real, freshly-provisioned backend with the real, already-configured Gemini key.

### Backend Committed, Frontend Scaffolded

Committed Project Writing Stages 5-8 plus both Stage 8 findings resolved this session (`4bad2bb`): `WorkItemRepository.requeue_stale_running` (general, kind-agnostic ADR-006 stale-`RUNNING` recovery) and `Conversation`/`Message`/`Message Context Link` implemented as originally decided (instructions persisted via Message, linked to the Draft Version they produce). Node.js was not installed on the machine; installed via winget with explicit confirmation before any frontend tooling ran. Scaffolded `frontend/` and built out Stages 1-7 of the plan in one pass (typed API client + Zod schemas for every backend endpoint, auth/session handling, workspace resolution, and pages for documents/knowledge search, writing-style upload/extraction, and the full draft generate/review/version loop) - each layer verified with typecheck/unit tests/build before moving to the next, rather than pausing per-stage, since the project owner supplied the full stack and plan up front (unlike the backend's own per-stage prompts).

### A Real Gap Found Before Any Page Could Work: Missing Read Endpoints

Building the typed API client surfaced that `agent` and `document` only ever had write endpoints - `POST /agents` and `POST /projects/{id}/documents` each reveal their created resource's ids once, with no way to retrieve them again, unlike every other write in this API. Flagged and, with explicit authorization, resolved by adding `GET /agents` (`GetAgentWorkspaceUseCase`) and `GET /projects/{id}/documents` (`ListProjectDocumentsUseCase`, reusing an existing but never-exposed repository method) - purely additive, mirroring `GET /writing/drafts`'s own pattern. 537/537 backend tests pass (13 new).

### Real Browser E2E Found Two Defects No Unit Test Ever Would

The first Playwright run against a real (non-AI) backend found: (1) **no CORS configuration existed at all** - every prior verification of this API used TestClient/curl, neither of which enforces CORS; a real browser blocked every cross-origin request outright. Fixed with `CORSMiddleware` + a new `Settings.cors_allowed_origins`. (2) **`AppShell`'s `<Outlet />` silently dropped React Router's outlet context** - it only propagates through the exact `<Outlet>` it's passed to, not transitively through a nested layout route's own `<Outlet>`, so every page nested under `AppShell` saw `undefined` instead of the resolved workspace and crashed the instant a real user reached one. Both fixed; the passing Playwright smoke test now guards against regressing either.

### The Real Manual Workflow Found Three Deeper SQLite Concurrency Defects

Ran the complete manual workflow through the real frontend, against a real backend with the real Gemini key, via a new `e2e/manual-workflow.spec.ts`. Found and fixed, in sequence:

1. **Stale reads across pooled SQLite connections** - a document's `processing_status` genuinely reached `processed` (real extraction+embedding committed), but an immediately-following real search on a different pooled connection returned zero results, confirmed moments later to actually be there via a separate request. Root cause: pysqlite's default implicit transaction handling, left at SQLAlchemy's default, is documented to interfere with transaction demarcation. Fixed via SQLAlchemy's own documented workaround (disable pysqlite's `isolation_level`, issue `BEGIN` explicitly).
2. **SQLite writer contention**, surfaced only once (1)'s fix made transactions correctly demarcated (and thus made concurrent-writer scenarios real for the first time): `database is locked` errors. Added `PRAGMA busy_timeout=30000` and WAL journal mode - the standard SQLite configuration for this pattern.
3. **A non-critical write with hard-failure handling** - every authenticated request also commits a session-activity timestamp (`AuthService.verify_token`); two requests firing in genuine parallel (the frontend routinely does this) can race for SQLite's single writer slot. This write was already documented as "must not affect whether a session is considered valid," but its failure handling hadn't matched that. Fixed by catching the transient lock specifically and continuing.

All three are covered by new/existing tests; 540/540 backend tests pass. **A deeper finding was reported, not further patched:** SQLite's single-writer ceiling (already known per ADR-004's own "Negative Consequences" item 1) is reachable within a single user's single session, not only at a future multi-user stage as originally framed - because a real frontend's normal parallel-request pattern is enough on its own. Full elimination needs a serialized-write strategy or the PostgreSQL migration ADR-004 already names, not further SQLite configuration; appended as a dated refinement to ADR-004.

### What Was Proven Working With Real Data

Across the investigation's several runs (each defect fixed in turn, each re-verified against a fresh database): real login, onboarding, document upload, real AI knowledge processing reaching `processed`, real semantic search returning the correct chunk immediately, real writing-style upload and extraction, real draft creation, **real first-version generation** with real evidence-linked content, evidence inspection, and review submission with confirmed immutability.

**Not completed as one single unbroken run:** the final pass's second generation (and the restart-persistence check) hit real Gemini free-tier rate limiting from the sheer number of real calls made across this investigation's repeated runs - external quota exhaustion from this session's own heavy testing, not a code defect (the identical code path already succeeded for the first generation in the same run). Stopped rather than continuing to burn quota once the investigation's actual goal - finding and fixing genuine defects - was satisfied.

### Documentation

`docs/Frontend_Implementation_Plan.md` created and updated throughout (stack decision, API contract, architecture decisions, staged plan, both endpoint/defect findings, what-was-built summary, and the full manual-workflow validation report). `docs/adr/ADR-004_Storage_and_Memory_Strategy.md` got a dated append-note refining its existing SQLite-concurrency risk item. The backend (`4bad2bb`) and the initial frontend milestone plus the CORS/AppShell fixes (`12578ed`) were committed earlier in this session; the three SQLite-concurrency fixes were committed separately (`d5f8e70`).

## Session: Stage 10 Closed Out Clean

**Objective:** with the three SQLite-concurrency fixes committed and real Gemini quota expected to have recovered, re-run the full manual workflow once more end to end and directly verify restart persistence, to close Stage 10 with a genuinely clean result rather than leaving it at "every piece independently proven."

**Full clean run.** Set up one more fresh, isolated environment (new SQLite DB, new storage root, the real Gemini key copied via shell redirection only) and ran `e2e/manual-workflow.spec.ts` once more. It passed in a single unbroken run (44s of real network/AI time): login, onboarding, document upload, real AI processing to `processed`, real search, writing-style upload, real style extraction, draft creation, **real Version 1 generation**, evidence inspection, review (`Request revisions`), **real Version 2 generation**, both versions confirmed present and distinct, approve, logout. Quota had indeed recovered since the investigation runs.

**Restart persistence, verified directly.** Stopped the backend process against the exact same database (no reset - a real restart) and restarted it, then re-authenticated and re-fetched every entity the run had touched: Agent/Project (identical ids and timestamps, confirming the original rows survived rather than being silently re-created), the Draft (still `approved`), both Draft Versions (identical content lengths and evidence-link counts), both Research Documents, a fresh real semantic search (still correct against the persisted embedding), and the Writing Profile (all 3 characteristics intact). Zero data loss, zero duplication.

**Failure/retry behavior** was already genuinely exercised (not simulated) during the earlier defect-hunting runs - the bounded-retry mechanism was observed both recovering a Work Item from a real transient failure and, separately, exhausting its 3 attempts into a terminal `failed` state under real rate-limit exhaustion - both the documented contract working as designed, and both paths already covered by the existing automated suite. Not re-simulated a third time through the browser, since deliberately crashing a request mid-flight isn't a real user interaction.

**Result:** Stage 9 and Stage 10 of the Frontend milestone plan are both complete. The one remaining open item in the whole plan is Stage 8 (AI/background-job UX polish - a dedicated failed-generation affordance in the UI itself, beyond the current plain pending/processing/processed/failed badges), left as a genuine, scoped follow-up. Temporary validation environment (a fourth fresh scratch DB/storage root this session) was stopped and deleted afterward, per this project's standing discipline.

Committed alongside this entry (small env-var configurability improvement to `e2e/manual-workflow.spec.ts`, plus this documentation).
